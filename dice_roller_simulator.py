# 🎲 Dice Roller Simulator
# Simulates rolling a dice (1–6).
# Optional: animate with sleep delays.

import random

input("Press Enter to roll the dice...")
print("You rolled a", random.randint(1, 6))
